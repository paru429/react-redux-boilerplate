import React from "react";
import './App.scss';

const App = ({text}) => {
  return (
    <div className="app">
      <div>Lets make this boilerplate ready!! {text}</div>
    </div>
  );
};

export default App;